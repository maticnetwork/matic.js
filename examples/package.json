{
  "name": "pos-sdk-examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Runnable consumer-shaped examples for @polygonlabs/pos-sdk. Deliberately OUTSIDE the pnpm workspace so imports resolve exactly as they would for an npm consumer; the SDK is wired via link: so `pnpm install` here always points at the local build.",
  "dependencies": {
    "@polygonlabs/pos-sdk": "link:../packages/pos-sdk",
    "ethers": "^6.13.0",
    "viem": "^2.21.0"
  }
}
