# @polygonlabs/pos-sdk

The changelog for `@maticnetwork/maticjs` (this package's 0.x/3.x
predecessor) lives in that package's own history on npm and in this
repository's git history prior to the 1.0 rewrite.
